machine:
  php:
    version: 7.0.4

dependencies:
  pre:
    - composer install
    - php example.php
    - rm /opt/circleci/php/$(phpenv global)/etc/conf.d/xdebug.ini

test:
  override:
    - php example.php