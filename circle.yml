machine:
  php:
    version: 7.0.4

dependencies:
  pre:
    - composer install
    - rm /opt/circleci/php/$(phpenv global)/etc/conf.d/xdebug.ini

test:
  override:
    - php example.php
    - cat data/ai/chat_logs/*

general:
  artifacts:
    - data/ai/chat_logs/* 